#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
* main - Entry point
 *
 * Descripcion: "Write a program  that prints the
 * lowercase alphabet in reverse"
 *
 * Return: Always zero success
*/

int main(void)
{
	int alphabet = 122;
		while (alphabet < 97)
		{
			putchar(alphabet);
		}
			putchar('\n');
			return (0);
}
