#include <stdio.h>

/**
 * main - Entry point
 *
 * Descripcion: "with proper grammar, but the outcome is a piece of art"
 *
 * Return: Always zero success
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
